# Application
NODE_ENV=development
PORT=3000
API_PREFIX=api/v1


# Neon/PostgreSQL connection string (preferred for Vercel/Neon)
DATABASE_URL=postgresql://neondb_owner:npg_wSo0OuGCKDv8@ep-jolly-voice-ah4d273q-pooler.c-3.us-east-1.aws.neon.tech/broadcast?sslmode=require&channel_binding=require

# Legacy fields (optional, fallback if DATABASE_URL not set)
DB_HOST=ep-jolly-voice-ah4d273q-pooler.c-3.us-east-1.aws.neon.tech
DB_PORT=5432
DB_USERNAME=neondb_owner
DB_PASSWORD=npg_wSo0OuGCKDv8
DB_DATABASE=broadcast

# Redis
# For local development, use localhost (docker-compose.db.yml exposes ports)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_TTL=3600

# Radio Browser API
RADIO_BROWSER_API_URL=https://de1.api.radio-browser.info

# CORS
# Support multiple origins: localhost for dev, GitHub Codespaces, and Vercel frontend deployment
# Pattern matches: https://{name}-{any-port}.app.github.dev, https://global-radio-podcast.vercel.app
CORS_ORIGIN=http://localhost:5173,https://*.app.github.dev,https://global-radio-podcast.vercel.app

# Rate Limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100
