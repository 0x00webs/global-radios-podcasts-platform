// Migration runner moved to project root: run `bun run migrate:run` from the backend/ folder.
console.warn('Use top-level run-migrations.ts instead. Run `bun run migrate:run` from the backend root.');
process.exit(0);
