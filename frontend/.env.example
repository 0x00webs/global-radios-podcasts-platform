# Backend API URL
# Leave empty to use Vite proxy in development
# In dev mode, requests to /api/* are proxied to the backend via vite.config.ts
# For production or explicit URLs, set this value
VITE_API_URL=
